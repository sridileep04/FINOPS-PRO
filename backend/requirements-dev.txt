-r requirements.txt

# --- Unit + integration testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-env==1.1.5
polyfactory==2.16.0
faker==30.3.0
freezegun==1.5.1

# Integration tests: real Postgres/Redis in disposable containers
testcontainers[postgres]==4.8.1

# HTTP-level API testing against the FastAPI app in-process
httpx==0.27.2

# Mock AWS APIs (STS/EC2/Cost Explorer) without hitting real AWS
moto[all]==5.0.14

# Mock outbound HTTP calls (e.g. backend -> steampipe-service)
respx==0.21.1

# OpenAPI-schema-driven fuzz/contract testing. Pinned to a 3.x release:
# 4.x dropped support for OpenAPI 3.1 schemas without the
# force_schema_version workaround used in tests/integration/test_api_contract.py,
# and 3.35.5 (an earlier try) predates that workaround's compatibility --
# 3.39.16 is the newest 3.x release, confirmed working against this
# app's OpenAPI 3.1 schema.
schemathesis==3.39.16

# --- Security static analysis ---
bandit==1.7.10
pip-audit==2.7.3