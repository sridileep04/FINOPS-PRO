# Bandit (Python SAST) configuration -- see .github/workflows/ci.yml,
# job "security", step "Bandit (Python SAST)" (`bandit -c .bandit -r app -f screen`).
#
# Skipped checks:
#   B608 - "hardcoded_sql_expressions" flags the f-string SQL assembly in
#          app/services/query_builder_service.py. That builder only
#          interpolates column/table identifiers that were already
#          resolved against an explicit allowlist (catalog.py) a few
#          lines above -- values are never interpolated directly into
#          the SQL string. This exact pattern has its own dedicated
#          regression suite (tests/unit/test_query_builder_security.py,
#          run as its own CI step) that asserts injection attempts are
#          rejected, so it's a reviewed false positive here rather than
#          an unvetted risk.
#   B105 - "hardcoded_password_string" fires on ISO-8601 timestamp
#          literals (e.g. "password_last_used": "2026-08-20T00:00:00Z")
#          in the mocked IAM-user fixture data used by
#          app/services/query_builder_sandbox.py. No actual credentials
#          are involved.
skips:
  - B608
  - B105